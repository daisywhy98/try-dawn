'use client';
import React from 'react'

const NewsCard = () => {
  return (
    <div>
        <button onClick={()=>{
            console.log('click')
        }

        }>try backend click</button>
    </div>
  )
}

export default NewsCard